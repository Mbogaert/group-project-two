<section>
    <h2>
        Your Matches
    </h2>
    {{!-- filter the blog posts in your area --}}
    <ul class="post-list">
        {{#each posts as |post|}}
        <li>
            {{> post-info.post }}
        </li>
        {{/each}}
    </ul>

</section>